{
  "name": "nimiq-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "pip install yt-dlp && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "yt-dlp-exec": "^3.0.0"
  }
}
